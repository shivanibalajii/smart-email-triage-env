[project]
name = "smart-email-triage"
version = "0.1.0"
description = "Smart email triage environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "inference:app"